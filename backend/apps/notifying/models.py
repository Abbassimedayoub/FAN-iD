"""
Bounded context `notifying` — intentionally empty shell.

No business table for this context is created yet. Models will be introduced
by the feature work that owns this context. This file exists so the Django app
is valid and the bounded-context boundary is visible in the codebase.
"""
